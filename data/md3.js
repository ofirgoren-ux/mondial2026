// data/md3.js - תשתית ריקה למחזור השלישי והמכריע של שלב הבתים
// יתמלא בנתונים עם פתיחת משחקי ההכרעה

const matchday3Database = {
    // הנתונים יוזרקו לכאן בהמשך...
};

if (typeof matchDatabase !== 'undefined') {
    Object.assign(matchDatabase, matchday3Database);
}